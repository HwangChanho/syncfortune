// app/src/lib/ui/heroCaps.ts — 전폭 히어로 높이 상한 **값만** (의존성 0)
// ─────────────────────────────────────────────────────────────────────────
// 왜 파일이 갈렸나: 훅(`heroSize.ts`)은 react-native 컴포넌트를 import 하므로
//   하네스(`check:herosize`)가 노드에서 읽을 수 없다. **값은 순수하게** 떼어 둔다.
//   ⇒ 하네스가 숫자를 '실행해서' 판정할 수 있다(주석이 아니라 값이 근거가 된다).
// ─────────────────────────────────────────────────────────────────────────

/** 배너 종류별 상한(px) — 여기 말고 화면에서 숫자를 정하지 말 것. */
export const HERO_CAP = {
  /** 유도 배너(다음 단계·무료 퍼널) — 누르라고 있는 것이라 크게 둘 이유가 없다. */
  banner: 300,
  /** 읽는 화면의 첫 이미지(콘텐츠 히어로) — 지면을 여는 역할이라 배너보다 넉넉히. */
  reading: 400,
} as const;

/** 세로로 긴 이미지(타로 카드 등)의 넓은 웹 최대 폭(px). */
export const PORTRAIT_MAX_WIDTH = 320;
