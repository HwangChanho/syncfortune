// app/src/lib/content/tenGodGroup.ts — 십신 → 5그룹 (순수 데이터 · 의존 0)
// ─────────────────────────────────────────────────────────────────────────
// ★왜 별도 파일인가 (2026-08-10)
//   이 표는 원래 `dailyFortune.ts` 안에 있었다. 그런데 그 파일은 `../i18n` 을 통해
//   **React Native 를 끌고 온다** → 표 하나 쓰려고 import 하면 헤드리스(tsx)에서 실행이 안 되고,
//   그러면 그 표를 쓰는 새 콘텐츠에 **골든을 붙일 수 없다.**
//   ⇒ 표만 여기로 뺀다. 소비처는 `dailyFortune`(오늘의 운세)·`hourFlow`(오늘의 시간대) 둘 다
//     여기서 가져다 쓴다 — 각자 적으면 "오늘 들어온 기운"이 두 화면에서 갈린다.
//     ([[duplicate-ui-single-source]] — 주석에 "같다"고 적는 건 보장이 아니다)
// ─────────────────────────────────────────────────────────────────────────

/** 십신 5그룹 — 통변의 최소 단위. */
export type TgGroup = '비겁' | '식상' | '재성' | '관성' | '인성';

/**
 * 십신 10정밀 → 5그룹.
 * ★관법 판정이 아니라 **정의**다(정재·편재는 둘 다 재성). 여기에 stance 는 없다.
 */
export const GROUP: Record<string, TgGroup> = {
  비견: '비겁', 겁재: '비겁',
  식신: '식상', 상관: '식상',
  정재: '재성', 편재: '재성',
  정관: '관성', 편관: '관성',
  정인: '인성', 편인: '인성',
};
