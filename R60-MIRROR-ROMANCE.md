# R60-MIRROR-ROMANCE — 경상명식(鏡像命式) 기반 이상형·실배우자 이원 분석

> ✅**규칙 번호 재배정 완료(daniel 2026-07-28)** — 초안의 `R48` 은 기존
> **R48(합충 < 생극제화 · 생극 먼저)** 과 겹쳤다. canonical(`interpretation/prompts/myeongri-core.ts`)이
> 그 뜻으로 R48 을 쓰고 LLM 프롬프트가 번호로 규칙을 참조하므로, 같은 번호로 두면
> **모델이 엉뚱한 규칙을 끌어다 쓴다**(2026-07-18 에 민감도 규칙이 R35 로 표기돼 시간층 R35 와 충돌한 전례).
> → 이 규칙은 **R60** 으로 확정(실측: knowledge·canonical 모두 R59 까지 사용, R60~ 비어 있음).

```yaml
rule_id: R60
rule_name: MIRROR-ROMANCE
version: 0.1.0-draft
layer: L1(도출) + L2(EEL 해석)
module: romance
status: SPEC_READY
depends_on: [R46-COMPATIBILITY, R-SPOUSE-DUAL, R7-SIPSEONG-GLYPH]
consumes: [natal_pillars, ilgan, wolji_gyeok, johu_index]
produces: [ideal_profile, realized_profile, gap_report]
golden_entry: PILOT_01 (甲戌/丁卯/辛丑/丁酉, M, 1994-03-16 酉시)
```

---

## 0. 목적

기존 연애 컨텐츠는 "배우자성(재성/관성) 하나"만 읽어서 단일 인물상을 뽑는다. 실사용자 피드백에서 반복적으로 나오는 불일치는 **"끌리는 사람"과 "실제로 만나는 사람"이 다르다**는 것이다.

R60은 원국 8자에 두 개의 변환을 적용해 서로 다른 두 개의 인물 프로파일을 생성한다.

| 변환 | 산출 명식 | 의미 | 심리 레이어 |
|---|---|---|---|
| **合鏡 (HAP_MIRROR)** | 8자 전부를 합신으로 치환 | **이상형** — 끌림, 무의식적 지향 | 욕망 / 투사 |
| **沖鏡 (CHUNG_MIRROR)** | 8자 전부를 충신으로 치환 | **실배우자** — 현실 인연, 실제 결합 | 사건 / 마찰 |

두 프로파일의 **차이(GAP)**가 이 룰의 최종 컨텐츠 페이로드다.

---

## 1. 이론적 근거

### 1.1 왜 合이 이상형인가

합은 **끌어당김(引)**이다. 원국의 각 글자가 "만나면 결합하고 싶어하는 상대"가 합신이다. 이는 사건이 아니라 **성향의 벡터**다.

일간 기준으로 검증하면:

| 일간 | 합신 | 십신 | 해석축 |
|---|---|---|---|
| 甲丙戊庚壬 (양간) | 己辛癸乙丁 | **정재** | 남명 배우자성 직결 → `IDEAL_AXIS=WEALTH` |
| 乙丁己辛癸 (음간) | 庚壬甲丙戊 | **정관** | 배우자성 아님 → `IDEAL_AXIS=OFFICER` |

**중요 (구현 시 분기 필수)**
- **양간 남명**: 합신 = 정재 = 배우자성. 이상형이 곧 배우자상의 연장선. GAP이 상대적으로 작다.
- **음간 남명**: 합신 = 정관 = 명예·사회적 자아. 이상형이 **"자기가 되고 싶은 상(像)의 투사"**로 나타난다. 여자를 고르는 게 아니라 자기 이상을 고르는 구조. GAP이 크게 벌어진다.
- 여명은 역으로 음간이 정관(배우자성 직결), 양간이 정재(재물·자기 관리축)가 된다.

이 분기가 GAP 크기를 결정하는 1차 변수다.

### 1.2 왜 沖이 실배우자인가

충은 **사건(動)**이다. 명리 실무에서 인연은 합보다 충·형에서 발생한다 — 배우자궁(일지)이 충을 받는 시기에 실제 만남·결혼·이별이 動한다. 합은 "상태", 충은 "이벤트"다.

일간 기준 검증:

| 일간군 | 충신 | 십신 | 해석축 |
|---|---|---|---|
| 甲乙丙丁 (木火) | 庚辛壬癸 | **편관** | 압박·경쟁을 통해 만남 → `REAL_AXIS=PRESSURE` |
| 庚辛壬癸 (金水) | 甲乙丙丁 | **편재** | 유동적 실연애·현실 인연 → `REAL_AXIS=FLOW` |
| 戊己 (土) | — | 無沖 | 특수 처리 (§2.3) |

金水 일간 남명은 충신이 곧 편재 = 실제 이성 인연 그 자체. 이 룰의 논리가 가장 깨끗하게 성립한다.
木火 일간 남명은 충신이 편관 = 압박축. "부딪히며 만난다 / 나를 긴장시키는 상대와 결합한다"로 해석 전환 필요.

### 1.3 한계 명시 (문서 상단 고지 필수)

- 경상명식은 **고전 원전에 없는 파생 기법**이다. 자평진전·적천수 어디에도 8자 전체 치환 기법은 없다.
- 따라서 R60 출력은 **결정론적 예언이 아니라 성향 대비 프레임**으로만 제시한다.
- 사주 자체 판정(격국/용신)에는 절대 개입시키지 않는다. R60은 romance 모듈 전용 파생 레이어다.

---

## 2. L1 결정적 도출

### 2.1 변환 테이블

```typescript
// 천간 오합 (완전 대합, involution)
export const GAN_HAP: Record<Gan, Gan> = {
  甲:'己', 己:'甲',
  乙:'庚', 庚:'乙',
  丙:'辛', 辛:'丙',
  丁:'壬', 壬:'丁',
  戊:'癸', 癸:'戊',
};

// 지지 육합 (완전 대합, involution)
export const JI_HAP: Record<Ji, Ji> = {
  子:'丑', 丑:'子',
  寅:'亥', 亥:'寅',
  卯:'戌', 戌:'卯',
  辰:'酉', 酉:'辰',
  巳:'申', 申:'巳',
  午:'未', 未:'午',
};

// 천간 상충 (4쌍만 성립. 戊己 거중무충)
export const GAN_CHUNG: Partial<Record<Gan, Gan>> = {
  甲:'庚', 庚:'甲',
  乙:'辛', 辛:'乙',
  丙:'壬', 壬:'丙',
  丁:'癸', 癸:'丁',
  // 戊, 己 → undefined
};

// 천간 무충 대체 (칠위상극 폴백)
export const GAN_CHUNG_FALLBACK: Record<'戊'|'己', Gan> = {
  戊:'甲',  // 甲剋戊, 편관
  己:'乙',  // 乙剋己, 편관
};

// 지지 육충 (완전 대충, involution)
export const JI_CHUNG: Record<Ji, Ji> = {
  子:'午', 午:'子',
  丑:'未', 未:'丑',
  寅:'申', 申:'寅',
  卯:'酉', 酉:'卯',
  辰:'戌', 戌:'辰',
  巳:'亥', 亥:'巳',
};
```

### 2.2 도출 함수

```typescript
type Pillar = { gan: Gan; ji: Ji };
type Chart = { year: Pillar; month: Pillar; day: Pillar; hour: Pillar };

function deriveHapMirror(natal: Chart): Chart {
  return mapPillars(natal, p => ({
    gan: GAN_HAP[p.gan],
    ji:  JI_HAP[p.ji],
  }));
}

function deriveChungMirror(natal: Chart): { chart: Chart; flags: string[] } {
  const flags: string[] = [];
  const chart = mapPillars(natal, (p, pos) => {
    let gan = GAN_CHUNG[p.gan];
    if (!gan) {
      gan = GAN_CHUNG_FALLBACK[p.gan as '戊'|'己'];
      flags.push(`GAN_NO_CHUNG:${pos}:${p.gan}`);
    }
    return { gan, ji: JI_CHUNG[p.ji] };
  });
  return { chart, flags };
}
```

### 2.3 유효성 보장

- **음양 패리티**: 천간합·지지합은 둘 다 음양을 반전시키므로 쌍으로 보존된다. 천간충·지지충은 둘 다 음양을 유지한다. → **두 경상명식 모두 항상 유효한 60갑자로 떨어진다.** 별도 검증 불필요하나, 단위 테스트로 60갑자 membership assert를 남길 것.
- **戊己 무충 케이스**: 폴백 적용 시 `GAN_NO_CHUNG` 플래그를 세우고, L2 해설에서 "해당 자리는 충이 성립하지 않아 극(剋) 관계로 대체 도출했다"는 문구를 반드시 노출한다. 신뢰도 점수 `confidence -= 0.15`.
- **대운 방향**: 경상명식의 년간 음양이 반전(합경) 또는 유지(충경)되므로 대운 순역이 달라진다. **단, R60은 경상명식의 대운을 사용하지 않는다.** 인물 프로파일 추출용 정지 스냅샷으로만 쓴다. 타이밍은 전부 원국 대운/세운(R-SPOUSE-DUAL)이 담당한다.

---

## 3. L2 프로파일 추출 (EEL)

경상명식을 **사람의 사주로 통변하지 않는다.** 인물 스타일 벡터만 뽑는다.

### 3.1 추출 차원

| 코드 | 차원 | 소스 | 산출 |
|---|---|---|---|
| `D1_TEMPER` | 기질 | 경상 일간 오행·음양 | 태양형/넝쿨형/바위형 등 |
| `D2_ROLE` | 사회적 상 | 경상 월지 격국 | 직업·역할 성향 |
| `D3_TEMP` | 정서 온도 | 조후(한난조습) 지수 | 따뜻함/서늘함 |
| `D4_BEHAVIOR` | 관계 행동 | 십신 편중 상위 2개 | 표현·주도·수용 패턴 |
| `D5_RISK` | 관계 리스크 | 합충형해 배치 | 마찰 지점 |
| `D6_IMPRESSION` | 인상 | 일간 오행 + 조후 | 외형 인상 (약하게, 단정 금지) |

### 3.2 십신 편중 → 행동 패턴 매핑 (요약본, 전체는 R7 테이블 참조)

```
비겁 과다  → 자기주장 강, 대등한 관계 요구, 경쟁적
식상 과다  → 표현 풍부, 자유 지향, 구속 거부
식상 부재  → 감정 표현 회로 부족, 결론만 말함, 내향 침잠
재성 과다  → 현실 감각, 관계를 관리·운용
관성 과다  → 규범적, 책임 중시, 관살혼잡 시 인연 다발
인성 과다  → 사유 깊음, 수용적, 편인 兩透 시 비주류 지식 지향
도식(편인剋식신) → 통찰 대비 산출량 적음, 표현 병목
```

### 3.3 GAP 산출 — 이 룰의 핵심

```typescript
type GapReport = {
  gap_score: number;        // 0.0 ~ 1.0
  divergent_dims: string[]; // 크게 갈리는 차원
  aligned_dims: string[];   // 일치하는 차원
  narrative_key: NarrativeKey;
};
```

**gap_score 계산**

```
gap_score = Σ(w_i × dist(ideal.D_i, real.D_i)) / Σ(w_i)

가중치:
  D1_TEMPER   0.25   (일간 오행 거리 — 상생 0.2 / 무관 0.5 / 상극 1.0)
  D3_TEMP     0.25   (조후 지수 차 정규화)
  D4_BEHAVIOR 0.30   (십신 편중 벡터 코사인 거리)
  D2_ROLE     0.10
  D5_RISK     0.10
```

**서사 키 분기**

| gap_score | key | 컨텐츠 골자 |
|---|---|---|
| < 0.30 | `ALIGNED` | 이상과 현실이 가까움. 끌린 사람과 결혼할 확률 높음. 대신 성장 자극이 적을 수 있음 |
| 0.30–0.60 | `TENSIONED` | 초기 끌림과 실제 관계의 결이 다름. 연애 초반 조정기가 반드시 옴 |
| > 0.60 | `INVERTED` | **이상형과 정반대 유형과 결합.** "왜 나는 늘 안 맞는 사람을 만나나"의 구조적 원인. 메인 훅 |

**음간 남명 / 양간 여명은 §1.1에 의해 gap_score에 +0.10 보정** (합신이 배우자성이 아니므로 이상형이 자기투사로 왜곡됨).

---

## 4. 출력 스키마

```json
{
  "rule_id": "R60",
  "confidence": 0.72,
  "flags": [],
  "ideal": {
    "mirror_pillars": "己卯/壬戌/丙子/壬辰",
    "ilgan": "丙",
    "axis": "OFFICER",
    "profile": {
      "D1_TEMPER": "태양형 — 존재감이 드러나고 밝음",
      "D2_ROLE": "칠살격+상관제살 — 압박을 논리로 되받는 전문직/리더형",
      "D3_TEMP": "온난 (조후지수 +0.4)",
      "D4_BEHAVIOR": ["관살혼잡→인연 다발", "상관투출→판단 빠르고 말로 정리"],
      "D5_RISK": ["子卯형 — 겉예의/속반발의 낙차", "辰戌충 — 주기적 대변동"],
      "D6_IMPRESSION": "밝고 시원한 인상, 첫인상에서 눈에 띔"
    }
  },
  "realized": {
    "mirror_pillars": "庚辰/癸酉/乙未/癸卯",
    "ilgan": "乙",
    "axis": "FLOW",
    "profile": {
      "D1_TEMPER": "넝쿨형 — 겉은 유연, 뿌리는 안 뽑힘",
      "D2_ROLE": "칠살격+살인상생 — 압박을 학습으로 전환하는 전문지식형",
      "D3_TEMP": "한랭 (조후지수 -0.6, 金寒水冷)",
      "D4_BEHAVIOR": ["편인양투+도식 → 사유 깊고 표현 적음", "관살혼잡 → 상대가 먼저 들어옴"],
      "D5_RISK": ["卯酉충 — 한계선 넘으면 점진적 소원이 아니라 절단", "조후 결핍 — 정서 표현 회로 부족"],
      "D6_IMPRESSION": "차분하고 서늘한 인상, 알수록 다르게 보임"
    }
  },
  "gap": {
    "gap_score": 0.71,
    "narrative_key": "INVERTED",
    "divergent_dims": ["D1_TEMPER", "D3_TEMP", "D4_BEHAVIOR"],
    "aligned_dims": ["D2_ROLE"],
    "headline": "끌리는 사람은 밝고 표현하는 쪽인데, 실제로 맺어지는 사람은 조용하고 안으로 깊은 쪽입니다."
  }
}
```

---

## 5. 통합 훅

### 5.1 R-SPOUSE-DUAL 연동

R-SPOUSE-DUAL이 담당하는 **원국 내부 배우자 지표 이원성**(월지 vs 일지)과 R60의 **경상 이원성**은 층위가 다르다. 혼동 금지.

```
R-SPOUSE-DUAL : 원국 안의 두 배우자 지표 → "어느 쪽이 실현되는가" (타이밍)
R60-MIRROR    : 원국 밖으로 투영된 두 인물상 → "어떤 사람인가" (프로파일)
```

**결합 로직**: R-SPOUSE-DUAL이 확정한 실현 시기(예: PILOT_01의 2027 丁未 발동 / 2029 己酉 귀결)에 R60의 `realized` 프로파일을 결합해 "이 시기에 나타나는 인물상"으로 렌더한다. `ideal` 프로파일은 시기와 무관한 상수로 취급.

### 5.2 R46 궁합 연동

실제 상대 명식이 입력되면, R46의 ρ 스코어와 별개로 R60 대비 지표를 추가 산출한다.

```typescript
partner_match = {
  ideal_similarity:    cosine(partnerProfile, ideal.profile),    // 0~1
  realized_similarity: cosine(partnerProfile, realized.profile), // 0~1
}
```

- `realized_similarity` 높고 `ideal_similarity` 낮음 → "머리로는 안 맞는데 실제로는 맞물리는 관계"
- 둘 다 높음 → 희귀 케이스, 강한 긍정 신호
- 둘 다 낮음 → R46 ρ 결과와 교차 검증 후 신중 서술

### 5.3 R46 극단 테스트 케이스

PILOT_01 기준 두 경상명식은 R46 검증에 유용하다.
- 충경(庚辰/癸酉/乙未/癸卯): 8자 전부 천극지충 → **간지 충돌도 최대, 십신 보완도 높음** (원국에 없던 壬癸 제살 성분 보유)
- 합경(己卯/壬戌/丙子/壬辰): 8자 전부 합 → **충돌도 최소, 기반(羈絆) 리스크 최대**

ρ 스코어링이 "충돌도"와 "보완도" 중 어느 쪽에 가중을 두는지 이 두 입력으로 드러난다. R46 회귀 테스트 픽스처로 등록 권장.

---

## 6. 가드레일

L2 프롬프트에 하드 제약으로 삽입할 것.

1. **인물 특정 금지** — 실존 인물, 특정 국적·직업·외모 단정 금지. "~한 결의 사람"까지만.
2. **결정론 금지** — "반드시 만난다 / 결혼한다" 금지. "이런 유형에 반복적으로 끌리는 구조" 수준.
3. **파생 기법 고지** — 출력 하단에 경상명식이 원전 기법이 아닌 SyncFortune 파생 프레임임을 1문장 명시.
4. **부정 서술 완화** — 실배우자 프로파일의 D5_RISK를 인신공격적으로 서술하지 않는다. "결함"이 아니라 "관계에서 조정이 필요한 지점".
5. **기혼자 분기** — 사용자가 기혼임을 입력한 경우 `realized` 프로파일은 "현재 배우자와의 관계 해석" 프레임으로 전환. 새 인연 예고로 렌더하지 않는다.
6. **단독 노출 금지** — R60만 단독으로 컨텐츠화하지 않는다. 반드시 원국 배우자성(R-SPOUSE-DUAL) 해석 뒤에 보조 레이어로 배치.

---

## 7. 골든 엔트리 — PILOT_01

```yaml
natal: 甲戌 / 丁卯 / 辛丑 / 丁酉
sex: M
ilgan: 辛 (음간) → IDEAL_AXIS = OFFICER, gap_bonus +0.10
ilgan_group: 金水 → REAL_AXIS = FLOW (충신 乙 = 편재)

hap_mirror:   己卯 / 壬戌 / 丙子 / 壬辰   # 일간 丙 = 원국 정관
chung_mirror: 庚辰 / 癸酉 / 乙未 / 癸卯   # 일간 乙 = 원국 편재

expected_gap_score: 0.68 ~ 0.75
expected_narrative_key: INVERTED
flags: []
```

**기대 서사 (회귀 테스트 기준 문장, 완전일치 아닌 의미일치 검증)**

> 이상형은 밝고 표현이 살아 있는 쪽 — 존재감이 드러나고, 압박 앞에서 논리로 되받는 유형.
> 실제 인연은 조용하고 서늘한 쪽 — 사유는 깊은데 표현이 적고, 부딪히면 서서히 멀어지는 게 아니라 한 번에 끊는 유형.
> 두 상이 온도·표현·기질 세 축에서 정면으로 갈린다.

**추가 검증 포인트**: 원국에 壬癸水(식상)가 천간 부재인데, 두 경상명식 모두 천간에 水가 투출(합경 壬壬 / 충경 癸癸)한다. R60은 이를 "원국 결핍 성분이 이성상으로 투영되는" 패턴으로 태깅할 것. 플래그 `DEFICIT_PROJECTION:水`. 이 패턴은 일반화 가능하니 §3에 D7 차원으로 승격 검토.

---

## 8. 구현 체크리스트

- [ ] `src/rules/r60/tables.ts` — 4개 변환 테이블 + 폴백
- [ ] `src/rules/r60/derive.ts` — deriveHapMirror / deriveChungMirror
- [ ] `src/rules/r60/profile.ts` — D1~D6 추출기 (기존 격국·조후 계산기 재사용)
- [ ] `src/rules/r60/gap.ts` — gap_score, narrative_key
- [ ] `src/rules/r60/prompt.ts` — L2 EEL 프롬프트 (§6 가드레일 하드코딩)
- [ ] `tests/r60/parity.test.ts` — 60갑자 유효성 (랜덤 1000 케이스)
- [ ] `tests/r60/involution.test.ts` — 합/충 테이블 대칭성 (戊己 예외)
- [ ] `tests/r60/golden_pilot01.test.ts` — §7 기대값
- [ ] `tests/r60/edge_musang.test.ts` — 戊/己 일간 무충 폴백 + 플래그
- [ ] R46 픽스처에 두 경상명식 등록
- [ ] romance 모듈 렌더 순서: R-SPOUSE-DUAL → R60 → (상대 명식 있으면) R46

---

## 9. 열린 이슈

1. **D7 결핍투영 차원** — 원국에 없는 오행이 경상명식 천간에 뜨는 패턴. PILOT_01에서 강하게 관측됨. 다른 명식에서도 재현되는지 샘플 확보 후 정식 차원 승격 결정.
2. **土 일간 처리** — 戊己는 무충이라 충경 도출이 폴백에 의존한다. 土 일간 사용자 비율이 낮지 않으므로 별도 검증 세트 필요. 대안으로 "土 일간은 지지충만 적용하고 천간은 유지" 안도 검토.
3. **격국론 vs 억부론 축** — 경상명식 프로파일을 뽑을 때 D2_ROLE을 격국 기준으로 잡을지 억부 용신 기준으로 잡을지. 현 스펙은 격국 기준. 기존 EEL 방법론 결정과 정합 맞출 것.
4. **여명 대칭성** — 본 스펙은 남명 기준으로 축을 정의했다. 여명 샘플로 IDEAL_AXIS/REAL_AXIS 매핑이 그대로 성립하는지 검증 필요.

---

## 10. 실배우자 인물 이미지 (daniel 2026-07-28 추가)

> daniel: "실제 만날 대상의 이미지도 나오면 좋을 것 같아. 실제 사진같이, 한국인이면 한국인 스타일,
> 일본인이면 일본인 스타일, 미국인이면 미국인 스타일."

### 10.0 먼저 — 이 기능의 위험과 설계 전제 (읽고 시작할 것)

이 기능은 **"당신이 만날 사람"이라며 사람 얼굴을 보여 준다.** 다른 이미지 기능과 성격이 다르다.

| 위험 | 왜 생기나 | 설계 대응 |
|---|---|---|
| **실존 인물 오인** | 사진처럼 생긴 얼굴은 "아는 사람 같다"로 읽힌다 → 특정인 지목·오해 | **사진 재현이 아니라 '사진 같은 일러스트'**(§10.3 톤). 카드에 *가상 이미지* 라벨 상시 노출 |
| 실제 인물 닮음 | 생성 모델이 유명인·실인물 얼굴을 재현할 수 있음 | 프롬프트에 인물명·유명인 레퍼런스 **절대 금지**. 생성 후 육안 검수 |
| 인종·외모 고정관념 | '국적 스타일'을 얼굴 특징으로 환원하면 편견이 된다 | 국적은 **사용자가 직접 고르는 옵션**(§10.1). 출생지·이름으로 추정하지 않는다 |
| 외모 단정 | §6-1 인물 특정 금지와 충돌 | 이미지에 **구체 이목구비를 근거로 제시하지 않는다** — D6_IMPRESSION(분위기)까지만 반영 |

★**확정(daniel 2026-07-28)**: **"사진처럼 또렷하되 명백히 생성 이미지인 톤"** 으로 간다.
완전 실사는 채택하지 않는다 — '실존 인물 오인'이 남고, 특정인 지목 민원은 앱에서 가장 비싼 문제가 된다.
`가상 이미지` 배지·고지(§10.4)는 **의무**다.

### 10.1 국적/외형 스타일 — 추정하지 않고 **고른다**

```typescript
type LookStyle = 'ko' | 'jp' | 'cn' | 'us' | 'eu' | 'sea' | 'unset';
```

- **기본값 = 앱 언어**(ko/ja/en → ko/jp/us). 사용자가 바꾸면 그 값이 우선.
- ⚠️**출생지·이름으로 추정하지 않는다.** 한국 출생 교포·이민자·국제커플이 즉시 어긋나고,
  "당신은 한국인이니까"라는 단정 자체가 불쾌감을 준다.
- `unset` = 이미지 대신 **분위기 카드**(실루엣·색·키워드)로 폴백. 이미지가 없어도 컨텐츠는 성립해야 한다.

### 10.2 생성 방식 — **원형 풀(pool) 사전 생성**, 런타임 생성 아님

기존 성격유형 이미지(240종)와 **같은 구조**를 재사용한다(`personaImages.ts` 패턴).

```
파일명: r60/<style>-<temper>-<temp>-<sex>-<variant>.jpg
  style   : ko | jp | cn | us | eu | sea
  temper  : D1_TEMPER 5종(木火土金水)
  temp    : D3_TEMP 3종(warm | neutral | cool)
  sex     : f | m
  variant : v1 | v2      ← 같은 칸에 2종(다양성)
→ 스타일당 5 × 3 × 2 × 2 = 60장 · 6스타일 전체 = 360장
```

⚠️**장수 정정(2026-07-28)**: 초안 §10.6 에 "ko 60장"이라고 적었는데 그건 계산 착오였다.
variant 없이 5×3×2 = **30칸**이라 ko 는 30장이 맞았다. daniel 이 승인한 60장에 맞추려면
칸마다 2종(variant)이 필요하고, 그게 제품에도 낫다 — 30장이면 "만날 사람"으로는 변별이 너무 적다.
**variant 선택도 결정론**: chart_id 해시 → v1/v2 고정(같은 사람에겐 항상 같은 얼굴).

- **런타임 생성 0** — 비용 0, 지연 0, 부적절 생성물 리스크 0(사전 육안 검수 통과분만 업로드).
- 선택은 **결정론**: `realized.profile` 의 D1/D3 + 사용자 style + 상대 성별 → 파일 1장.
  같은 사람에게 항상 같은 얼굴이 나온다(매번 바뀌면 신뢰가 깨진다). variant 는 chart_id 해시로 고정.
- 업로드 = 공개 버킷 `persona` 와 같은 방식(스토리지 교체만으로 재빌드 없이 갱신).

⚠️상대 성별: 사용자 성별의 반대로 **자동 가정하지 않는다.** 명식에 성별이 있어도
지향은 별개다 → `partnerSex` 를 사용자가 고르게 하고(기본=미지정), 미지정이면 §10.1 폴백 카드.

### 10.3 톤 레시피 (SDXL — 기존 이미지 파이프 재사용)

```
공통 프리픽스(구도 먼저 — 07-19 교훈: 구도 지시를 맨 앞에 두지 않으면 무시된다):
  "upper body portrait, head and shoulders only, centered, plain soft background,
   natural window light, shallow depth of field, editorial portrait photography style,
   soft film grain"

금지(negative):
  "celebrity, famous person, specific individual, logo, text, watermark,
   hands, fingers, full body, group, nsfw, exaggerated features"
```

- **손·전신 제외**(07-19 실측: 손가락 기형이 가장 잦은 실패) → 흉상 크롭 고정.
- 국적 스타일은 **의상·헤어·분위기·조명**으로 표현하고, 인종 특징을 나열하지 않는다.
- D3_TEMP(온난/한랭)는 **색온도**로, D1_TEMPER 는 **자세·시선**으로 매핑(얼굴 생김새 아님).

### 10.4 화면 표기 (필수)

카드에 아래 세 가지가 **항상** 보여야 한다. 하나라도 빠지면 오인 위험이 커진다.

1. `가상 이미지` 배지 (이미지 위, 상시)
2. 캡션: "실존 인물이 아니라, 명식에서 읽은 **분위기**를 그린 그림이에요."
3. 하단 고지: §6-3 파생 기법 고지와 한 묶음으로

### 10.5 구현 체크리스트 (§8 에 이어서)

- [ ] `LookStyle` 선택 UI(기본=앱 언어) + `partnerSex` 선택 · 둘 다 **선택 안 함** 허용
- [ ] `src/lib/content/r60Images.ts` — (style, temper, temp, sex) → URL (personaImages.ts 동형)
- [ ] 원형 풀 **ko 60장 먼저** 생성·**육안 검수**·스토리지 업로드 (daniel 확정 · 나머지 5스타일은 이후)
- [ ] 폴백: style/partnerSex 미지정 또는 URL 404 → 분위기 카드(실루엣+키워드)
- [ ] `check:r60image` 하네스 — ①풀 조합 누락 0 ②모든 카드에 '가상 이미지' 배지·고지 존재
      ③국적을 **추정하는 코드가 없다**(birthPlace/이름 기반 분기 금지)

### 10.6 열린 이슈 추가

5. ~~실사 수위~~ → **확정**: 사진 같은 일러스트 톤 + 배지·고지 의무(daniel 2026-07-28)
6. ~~출시 범위~~ → **확정**: **ko 60장(5×3×2×2)부터**. jp/us 등 나머지 5스타일은 ko 반응 확인 후(daniel 2026-07-28)
7. ★**D3_TEMP(조후 지수) 계산식 미확정 — 구현하며 발견(2026-07-28)**
   §3.1 은 "조후(한난조습) 지수"라고만 하고 **식이 없다.** §4 예시는 이상형 +0.4 / 실배우자 -0.6(차 1.0)인데,
   내가 잡은 식(천간 + 지지본기 8자의 한난 가중 평균)으로 PILOT_01 을 돌리면 **-0.2 / -0.25(차 0.05)** 가 나온다.
   그 결과 gap_score 가 0.5(TENSIONED)로, §7 기대(0.68~0.75 · INVERTED)와 어긋난다.
   ⚠️**기대값에 맞춰 가중치를 역산하지 않았다** — 그건 보상해킹이다(CLAUDE.md 규칙7).
   → daniel 확정 필요: 조후를 **월지(계절) 기준**으로 볼지, 8자 평균으로 볼지, 지장간까지 볼지.
   확정 전까지 골든은 현재 구현값(0.5/TENSIONED)을 고정해 회귀만 막는다.
8. **style 미지정 사용자 비율** — 기본값(앱 언어)을 그대로 쓰는 비율이 높으면 ko 외 스타일 우선순위가 바뀐다. 출시 후 실측.

